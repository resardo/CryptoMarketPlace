//Crypto MarketPlace

// To run the application click on the run button in the VisulStudioCode IDE

// You need to wait for some seconds until the data are loaded

// If a table with no data is displayed check internet connection

// If a table with data is displayed then you can filter the current table by "Symbol" column,
by putting a string in the input text placed after "Search By Symbol" label.

//After inputing the string click on the search button and a table with filtered data will be displayed.

//If you want to see all the data click on "Back to full list"
